1 主线程
	a、全局变量 
		global.master
	b、执行流程
		命令监听初始化--监听工作进程事件
		启动打印进程
		启动控制台输入进程
		初始化任务队例
		设置初始化完成标志	---可省略，单线程nodejs事件响应肯定在此之后。
2 命令线程
	a、监听输入命令，传送给主线程
	b、执行命令，汇报状态
3 命令源线程
	a、获取命令，传送给主线程
	b、执行命令，汇报状态
4 打印线程
    a、执行命令，汇报状态
	
	
	主程序	各子进程对应一个命令栈，一个执行成功命令队例，一个失败命令队例
			打印队例
			
			

输出：
	
			
			